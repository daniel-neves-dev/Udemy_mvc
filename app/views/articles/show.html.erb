<h2>Show article</h2>

<h3><%= @article.title %></h3>
<p><%= @article.description %></p>

<%= link_to "Articles index", root_path %>

<%= link_to "Edit article", edit_article_path(@article) %>

<li><%= link_to "Delete", article_path(@article), data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %></li>
